{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"local-n8n","text":"<p><code>local-n8n</code> provides the <code>lon</code> CLI for running a local, self-hosted n8n instance with a path toward encrypted, portable backups.</p> <p>The current release is an alpha. It focuses on the local lifecycle:</p> <ul> <li>create and start an n8n Docker Compose instance</li> <li>initialize a new instance with a guided first-run command</li> <li>stop, start, restart, and remove the container while keeping data</li> <li>inspect status and logs</li> <li>manage named local instances</li> <li>run read-only prerequisite checks</li> <li>use script-friendly global flags such as <code>--json</code> and <code>--dry-run</code></li> </ul>"},{"location":"#install","title":"Install","text":"<p>Install the current GitHub prerelease with <code>uv</code>:</p> <pre><code>uv tool install git+https://github.com/harihkim/local-n8n.git@v0.1.0a2\n</code></pre> <p>For development inside a checkout, use:</p> <p><code>bash test uv run lon --help</code></p>"},{"location":"#start-here","title":"Start Here","text":"<p>New users should begin with the Quickstart.</p> <p>If you are scripting against the CLI, read the Command Reference. If something feels off, check Troubleshooting.</p>"},{"location":"#versioned-docs","title":"Versioned Docs","text":"<p>The documentation site is versioned:</p> <ul> <li><code>latest</code> is the default and follows the newest released docs.</li> <li><code>dev</code> follows unreleased work on <code>main</code>.</li> <li>release versions such as <code>v0.1.0a2</code> remain browsable.</li> </ul> <p>For normal use, stay on <code>latest</code>.</p>"},{"location":"commands/","title":"Command Reference","text":"<p>Global options must be placed before the command:</p> <p>```bash test uv run lon --help <pre><code>## Global Options\n\n| Option | Purpose |\n| --- | --- |\n| `--verbose` | Print diagnostic details to stderr. |\n| `--json` | Emit one JSON object to stdout for finite commands. |\n| `--dry-run` | Preview mutating/browser commands without side effects. |\n| `--yes`, `-y` | Assume yes for confirmation prompts where supported. |\n\nHuman output goes to stderr. JSON output goes to stdout. Lifecycle commands that may wait on Docker\nalso stream Docker Compose output to stderr. In an interactive terminal, Docker's live progress display is\npreserved; in non-interactive output it falls back to plain streaming.\n\n## Lifecycle\n\n### `lon init`\n\nPlan, initialize, start, and optionally open a local n8n instance. This is the guided first-run entry\npoint for new users.\n\nOptions:\n\n- `--instance`, `-i`: instance name. Default: `default`.\n- `--port`, `-p`: host port for n8n.\n- `--open` / `--no-open`: open the n8n web UI after startup. Default: `--open`.\n\nExamples:\n\n```bash\nlon init\nlon init --instance manual-check --port 5683 --no-open\n</code></pre></p> <p>Safe preview:</p> <p>```bash test uv run lon --dry-run init --instance preview --port 5688 --no-open <pre><code>JSON preview:\n\n```bash test\nuv run lon --json --dry-run init --instance preview --port 5688 --no-open\n</code></pre></p> <p>On first run, n8n may redirect to <code>/setup</code>; create the local owner account there.</p>"},{"location":"commands/#lon-up","title":"<code>lon up</code>","text":"<p>Render instance files if needed, create or recreate the Compose container, start n8n, and wait until the n8n web UI is reachable.</p> <p>Options:</p> <ul> <li><code>--instance</code>, <code>-i</code>: instance name. Default: <code>default</code>.</li> <li><code>--port</code>, <code>-p</code>: host port for n8n.</li> </ul> <p>Examples:</p> <pre><code>lon up\nlon up --instance manual-check --port 5683\n</code></pre> <p>Safe preview:</p> <p>```bash test uv run lon --dry-run up --instance preview --port 5688 <pre><code>If local state still has the old built-in `1.113.3` image pin, `lon up` prompts before moving that instance\nto n8n's stable image reference. Press Enter to accept the default `yes`, or type `n` to keep the existing\nimage. Custom image references are not changed.\n\nJSON preview:\n\n```bash test\nuv run lon --json --dry-run up --instance preview --port 5688\n</code></pre></p>"},{"location":"commands/#lon-down","title":"<code>lon down</code>","text":"<p>Remove the n8n container and network while keeping the Docker data volume.</p> <p>Options:</p> <ul> <li><code>--instance</code>, <code>-i</code>: instance name. Default: <code>default</code>.</li> </ul> <p>Use <code>lon up</code> to create and start the container again.</p>"},{"location":"commands/#lon-stop","title":"<code>lon stop</code>","text":"<p>Stop the existing n8n container while keeping the container and Docker volume.</p> <p>Options:</p> <ul> <li><code>--instance</code>, <code>-i</code>: instance name. Default: <code>default</code>.</li> </ul> <p>Use <code>lon start</code> to resume the stopped container.</p>"},{"location":"commands/#lon-start","title":"<code>lon start</code>","text":"<p>Start an existing stopped n8n container and wait for the web UI.</p> <p>Options:</p> <ul> <li><code>--instance</code>, <code>-i</code>: instance name. Default: <code>default</code>.</li> </ul> <p>If <code>lon down</code> removed the container, use <code>lon up</code> instead.</p>"},{"location":"commands/#lon-restart","title":"<code>lon restart</code>","text":"<p>Restart an existing n8n container and wait for the web UI.</p> <p>Options:</p> <ul> <li><code>--instance</code>, <code>-i</code>: instance name. Default: <code>default</code>.</li> </ul> <p>If the container is not present, <code>restart</code> fails fast and suggests <code>lon up</code>.</p>"},{"location":"commands/#inspection","title":"Inspection","text":""},{"location":"commands/#lon-status","title":"<code>lon status</code>","text":"<p>Show URL, container state, web UI reachability, Docker volume, and Compose file path for one instance.</p> <p>Options:</p> <ul> <li><code>--instance</code>, <code>-i</code>: instance name. Default: <code>default</code>.</li> </ul> <p>Examples:</p> <pre><code>lon status\nlon --json status --instance manual-check\n</code></pre> <p>Help:</p> <p>```bash test uv run lon status --help <pre><code>### `lon list`\n\nList registered instances with name, URL, container state, and volume.\n\nExamples:\n\n```bash\nlon list\nlon --json list\n</code></pre></p>"},{"location":"commands/#lon-logs","title":"<code>lon logs</code>","text":"<p>Show n8n container logs.</p> <p>Options:</p> <ul> <li><code>--instance</code>, <code>-i</code>: instance name. Default: <code>default</code>.</li> <li><code>--follow</code>, <code>-f</code>: follow log output.</li> <li><code>--tail</code>: number of log lines to show. Default: <code>100</code>.</li> </ul> <p>Examples:</p> <pre><code>lon logs\nlon logs --tail 50\nlon logs --follow\n</code></pre> <p>Current limitation: <code>lon --json logs --follow</code> is rejected because streaming JSON needs a separate newline-delimited output contract.</p>"},{"location":"commands/#lon-open","title":"<code>lon open</code>","text":"<p>Open the n8n web UI URL in a browser if possible.</p> <p>Options:</p> <ul> <li><code>--instance</code>, <code>-i</code>: instance name. Default: <code>default</code>.</li> </ul> <p>On WSL, <code>lon open</code> tries <code>wslview</code> and then <code>powershell.exe Start-Process</code>. On Linux it tries <code>xdg-open</code>; on macOS it tries <code>open</code>.</p> <p>If no opener is available, it prints the URL.</p>"},{"location":"commands/#diagnostics","title":"Diagnostics","text":""},{"location":"commands/#lon-doctor","title":"<code>lon doctor</code>","text":"<p>Run read-only prerequisite diagnostics.</p> <p>Options:</p> <ul> <li><code>--port</code>, <code>-p</code>: port to check. Default: <code>5678</code>.</li> </ul> <p>Checks:</p> <ul> <li>platform</li> <li>Docker CLI</li> <li>Docker daemon</li> <li>Docker backend, including Docker Desktop WSL integration detection</li> <li>Docker Compose</li> <li>port availability</li> </ul> <p>Help:</p> <p>```bash test uv run lon doctor --help <pre><code>## Development\n\n### `lon dev wipe`\n\nDevelopment-only destructive reset. Removes local-n8n Docker resources and local instance state for the\ncurrent `$LOCAL_N8N_HOME`.\n\nThis is not a normal user cleanup command. It is intended for local development when you want a clean slate.\n\nOptions:\n\n- `--yes`, `-y`: skip the typed confirmation prompt.\n- `--images`: also remove known local-n8n Docker images, including the current built-in n8n image.\n\nSafe preview:\n\n```bash test\nuv run lon --dry-run dev wipe\nuv run lon --dry-run dev wipe --images\n</code></pre></p> <p>Run for real:</p> <pre><code>lon dev wipe\nlon dev wipe --images\n</code></pre> <p>For real deletion, the command warns and asks you to type <code>yes</code>. Press Enter or type anything else to keep the default <code>no</code> choice. Use <code>--yes</code> only for development automation.</p> <p>It removes:</p> <ul> <li><code>local-n8n-*</code> Compose projects/containers/networks found through registered instances or instance dirs</li> <li>Docker volumes referenced by local-n8n state or instance naming</li> <li>known local-n8n Docker images when <code>--images</code> is passed</li> <li><code>$LOCAL_N8N_HOME/instances</code></li> <li><code>$LOCAL_N8N_HOME/state.db</code> and SQLite sidecar files</li> </ul>"},{"location":"development/","title":"Development","text":"<p>Development currently targets Linux or WSL with Python 3.13 and <code>uv</code>.</p>"},{"location":"development/#setup","title":"Setup","text":"<pre><code>git clone https://github.com/harihkim/local-n8n.git\ncd local-n8n\nuv run --python 3.13 lon --help\n</code></pre>"},{"location":"development/#checks","title":"Checks","text":"<p>Run the same checks CI runs:</p> <pre><code>uv run --python 3.13 ruff check .\nuv run --python 3.13 ruff format --check .\nuv run --python 3.13 ty check\nuv run --python 3.13 pyrefly check\nuv run --python 3.13 pytest tests\nuv run --python 3.13 mkdocs build --strict\n</code></pre> <p>The project config enables Pyrefly's <code>strict</code> preset and configures ty to treat all enabled rules as errors for <code>src</code> and <code>tests</code>.</p>"},{"location":"development/#docs-drift-checks","title":"Docs Drift Checks","text":"<p>The test suite includes documentation checks that verify:</p> <ul> <li>every current CLI command is documented in <code>docs/commands.md</code></li> <li>global options are documented</li> <li>key command options are documented</li> <li>fenced <code>bash test</code> examples in docs still run</li> </ul> <p>Use <code>bash test</code> only for safe commands that do not start Docker or open a browser.</p> <p>Use plain <code>bash</code> for examples intended for humans but not CI.</p>"},{"location":"development/#branch-and-pr-flow","title":"Branch and PR Flow","text":"<p>Use small branches and pull requests:</p> <pre><code>git switch -c docs-site-versioned\n</code></pre> <p>Before merging, wait for CI and docs checks to pass.</p>"},{"location":"development/#local-docs-preview","title":"Local Docs Preview","text":"<pre><code>uv run --python 3.13 mkdocs serve\n</code></pre> <p>The local preview usually runs at:</p> <pre><code>http://127.0.0.1:8000/\n</code></pre>"},{"location":"manual-testing/","title":"Manual Testing","text":"<p>Use this checklist when validating a release candidate locally.</p>"},{"location":"manual-testing/#baseline","title":"Baseline","text":"<pre><code>uv run lon --help\nuv run lon doctor\nuv run lon --dry-run init --instance preview --port 5688 --no-open\n</code></pre> <p>Expected:</p> <ul> <li>help lists all current commands</li> <li><code>doctor</code> reports platform, Docker CLI, Docker daemon, Docker backend, Docker Compose, and port state</li> <li><code>init</code> dry-run explains planned writes/start/open behavior without side effects</li> </ul>"},{"location":"manual-testing/#default-instance","title":"Default Instance","text":"<pre><code>uv run lon init --no-open\nuv run lon status\nuv run lon open\n</code></pre> <p>Expected:</p> <ul> <li><code>init</code> prints prerequisite/startup progress</li> <li>success is printed only after the n8n web UI is reachable</li> <li><code>status</code> shows <code>Container</code> and <code>Web UI</code></li> <li>first-run n8n may redirect to <code>/setup</code></li> </ul> <p>Clean up:</p> <pre><code>uv run lon down\n</code></pre>"},{"location":"manual-testing/#named-instance","title":"Named Instance","text":"<pre><code>uv run lon init --instance manual-check --port 5683 --no-open\nuv run lon status --instance manual-check\nuv run lon list\n</code></pre> <p>Expected:</p> <ul> <li><code>manual-check</code> uses <code>http://localhost:5683</code></li> <li><code>lon list</code> shows the instance name and container state</li> <li><code>lon status</code> without <code>--instance manual-check</code> still checks <code>default</code></li> </ul> <p>Clean up:</p> <pre><code>uv run lon down --instance manual-check\n</code></pre>"},{"location":"manual-testing/#stop-start-down","title":"Stop, Start, Down","text":"<pre><code>uv run lon up --instance lifecycle-check --port 5684\nuv run lon stop --instance lifecycle-check\nuv run lon status --instance lifecycle-check\nuv run lon start --instance lifecycle-check\nuv run lon down --instance lifecycle-check\nuv run lon restart --instance lifecycle-check\n</code></pre> <p>Expected:</p> <ul> <li>after <code>stop</code>, <code>status</code> shows the container as stopped/exited</li> <li>after <code>down</code>, <code>status</code> shows the container as not present</li> <li><code>restart</code> after <code>down</code> fails fast and suggests <code>lon up</code></li> </ul>"},{"location":"manual-testing/#global-flags","title":"Global Flags","text":"<p>Safe dry-run:</p> <p>```bash test uv run lon --dry-run up --instance preview --port 5688 <pre><code>JSON dry-run:\n\n```bash test\nuv run lon --json --dry-run up --instance preview --port 5688\n</code></pre></p> <p>Verbose status:</p> <pre><code>uv run lon --verbose status\n</code></pre> <p>Expected:</p> <ul> <li>dry-run does not write instance files, state, or run Docker</li> <li>JSON output is a single object on stdout</li> <li>verbose diagnostics go to stderr</li> </ul>"},{"location":"manual-testing/#development-wipe","title":"Development Wipe","text":"<p>Preview only:</p> <p>```bash test uv run lon --dry-run dev wipe uv run lon --dry-run dev wipe --images <pre><code>Expected:\n\n- dry-run does not remove Docker resources or local files\n- real deletion warns and asks you to type `yes`\n- pressing Enter keeps the default `no` choice and deletes nothing\n\n## Legacy Image Update Prompt\n\nIf an instance was created with the earlier built-in `1.113.3` image pin, run:\n\n```bash\nuv run lon up\n</code></pre></p> <p>Expected:</p> <ul> <li>the CLI prints the current image and the new stable image reference</li> <li>the prompt is <code>Update n8n image now? (Y/n):</code></li> <li>pressing Enter accepts the default <code>yes</code></li> <li>typing <code>n</code> cancels before Docker Compose runs</li> </ul>"},{"location":"quickstart/","title":"Quickstart","text":"<p>This guide assumes Linux or WSL with Python 3.13, <code>uv</code>, and Docker Engine available.</p> <p>Automatic WSL and Docker provisioning is planned for a later phase. For now, <code>lon doctor</code> detects and explains prerequisite problems.</p>"},{"location":"quickstart/#1-install","title":"1. Install","text":"<pre><code>uv tool install git+https://github.com/harihkim/local-n8n.git@v0.1.0a2\n</code></pre> <p>Inside the repository checkout, use:</p> <p>```bash test uv run lon --help <pre><code>## 2. Check Prerequisites\n\n```bash\nlon doctor\n</code></pre></p> <p><code>doctor</code> is read-only. It checks platform, Docker CLI, Docker daemon, Docker backend, Docker Compose, and port availability. Inside WSL, it also reports when Docker Desktop WSL integration is the active backend.</p>"},{"location":"quickstart/#3-initialize-and-start-n8n","title":"3. Initialize and Start n8n","text":"<pre><code>lon init\n</code></pre> <p>This creates the default instance under:</p> <pre><code>~/.config/local-n8n/instances/default/\n</code></pre> <p>It writes:</p> <ul> <li><code>docker-compose.yml</code></li> <li><code>.env</code> with <code>N8N_ENCRYPTION_KEY</code></li> </ul> <p>The <code>.env</code> file is created once and preserved on later runs.</p> <p><code>init</code> starts n8n, opens the browser when possible, and explains the local owner setup step. The generated Compose file uses n8n's official stable Docker image, <code>docker.n8n.io/n8nio/n8n</code>.</p>"},{"location":"quickstart/#4-open-n8n","title":"4. Open n8n","text":"<pre><code>lon open\n</code></pre> <p>Or open the URL printed by <code>lon up</code>, usually:</p> <pre><code>http://localhost:5678\n</code></pre> <p>On first run, n8n may redirect to <code>/setup</code>. That is expected.</p>"},{"location":"quickstart/#5-stop-or-remove-the-container","title":"5. Stop or Remove the Container","text":"<p>Stop keeps the container available for <code>start</code>:</p> <pre><code>lon stop\nlon start\n</code></pre> <p>Down removes the container/network but keeps the Docker volume:</p> <pre><code>lon down\nlon up\n</code></pre>"},{"location":"quickstart/#named-instances","title":"Named Instances","text":"<p>Use <code>--instance</code> to create separate local instances:</p> <pre><code>lon init --instance manual-check --port 5683\nlon status --instance manual-check\nlon down --instance manual-check\n</code></pre> <p>Instance names must use lowercase letters, digits, and hyphens. They must start and end with a letter or digit.</p>"},{"location":"release/","title":"Release Process","text":"<p><code>local-n8n</code> currently publishes GitHub prereleases, not PyPI packages.</p> <p>PyPI publishing is deferred until the core backup/restore MVP loop is stable.</p>"},{"location":"release/#package-release","title":"Package Release","text":"<p>From a clean <code>main</code>:</p> <pre><code>git tag -a v0.1.0a2 -m \"v0.1.0a2\"\ngit push origin v0.1.0a2\n</code></pre> <p>The release workflow runs checks, builds:</p> <ul> <li>wheel</li> <li>source distribution</li> </ul> <p>and creates a GitHub prerelease with those artifacts attached.</p>"},{"location":"release/#install-from-a-release","title":"Install From a Release","text":"<pre><code>uv tool install git+https://github.com/harihkim/local-n8n.git@v0.1.0a2\n</code></pre>"},{"location":"release/#docs-release","title":"Docs Release","text":"<p>Docs are published with <code>mike</code>.</p> <p>The default docs version is <code>latest</code>, which points at the newest released docs.</p> <p>Useful commands:</p> <pre><code>uv run --python 3.13 mike deploy --push --update-aliases v0.1.0a2 latest\nuv run --python 3.13 mike set-default --push latest\n</code></pre> <p>Development docs can be published separately:</p> <pre><code>uv run --python 3.13 mike deploy --push dev\n</code></pre> <p>Expected public URL:</p> <pre><code>https://harihkim.github.io/local-n8n/\n</code></pre>"},{"location":"troubleshooting/","title":"Troubleshooting","text":""},{"location":"troubleshooting/#docker-was-not-found","title":"Docker Was Not Found","text":"<p>Run:</p> <pre><code>lon doctor\n</code></pre> <p>If Docker CLI is missing, install Docker Desktop for Windows and enable WSL integration for this distro, or install Docker Engine directly inside WSL/Linux.</p>"},{"location":"troubleshooting/#docker-desktop-wsl-integration-is-active","title":"Docker Desktop WSL Integration Is Active","text":"<p>This is a valid setup. Docker Desktop runs its own WSL backend and exposes Docker commands inside your integrated WSL distro.</p> <p>If <code>lon doctor</code> reports Docker Desktop WSL integration as the active backend, Docker resources are managed by Docker Desktop rather than by a Docker Engine installed directly inside the current distro.</p>"},{"location":"troubleshooting/#docker-desktop-wsl-integration-is-off","title":"Docker Desktop WSL Integration Is Off","text":"<p>If Docker Desktop is installed but <code>docker</code> is missing or the daemon is unreachable inside WSL, enable integration for this distro:</p> <ol> <li>Open Docker Desktop on Windows.</li> <li>Go to Settings &gt; Resources &gt; WSL Integration.</li> <li>Enable integration for your WSL distro.</li> <li>Apply the change and re-run:</li> </ol> <pre><code>lon doctor\n</code></pre> <p>Alternatively, install Docker Engine directly inside WSL if you do not want to use Docker Desktop.</p>"},{"location":"troubleshooting/#docker-daemon-is-not-running","title":"Docker Daemon Is Not Running","text":"<p>If <code>doctor</code> reports that Docker is installed but unreachable, start Docker Desktop or Docker Engine and retry.</p> <p>Under WSL with Docker Engine installed directly, this may look like:</p> <pre><code>sudo service docker start\n</code></pre>"},{"location":"troubleshooting/#port-is-already-in-use","title":"Port Is Already in Use","text":"<p>Default n8n port is <code>5678</code>. Choose another port:</p> <pre><code>lon up --instance manual-check --port 5683\n</code></pre> <p>Then open:</p> <pre><code>http://localhost:5683\n</code></pre>"},{"location":"troubleshooting/#browser-shows-connection-failed-right-after-lon-up","title":"Browser Shows Connection Failed Right After <code>lon up</code>","text":"<p><code>lon up</code> waits for the n8n web UI before printing success. If the browser still fails:</p> <ul> <li>run <code>lon status</code></li> <li>run <code>lon logs</code></li> <li>retry the URL after a few seconds</li> <li>check whether another process owns the port</li> </ul>"},{"location":"troubleshooting/#browser-shows-setup","title":"Browser Shows <code>/setup</code>","text":"<p>That is normal for a fresh n8n instance. It means n8n is ready for first-run owner setup.</p>"},{"location":"troubleshooting/#browser-shows-cannot-get","title":"Browser Shows <code>Cannot GET /</code>","text":"<p>This should not count as ready. <code>lon</code> rejects generic <code>Cannot GET ...</code> responses in its readiness check.</p> <p>If you see it manually, run:</p> <pre><code>lon logs\nlon status\n</code></pre>"},{"location":"troubleshooting/#restart-fails-after-down","title":"<code>restart</code> Fails After <code>down</code>","text":"<p><code>lon down</code> removes the container. It keeps the Docker volume, but there is no container left to restart.</p> <p>Use:</p> <pre><code>lon up\n</code></pre> <p>If you want to stop without removing the container, use:</p> <pre><code>lon stop\nlon start\n</code></pre>"},{"location":"troubleshooting/#named-instance-confusion","title":"Named Instance Confusion","text":"<p>Commands default to the <code>default</code> instance.</p> <p>If you created an instance with:</p> <pre><code>lon up --instance manual-check --port 5683\n</code></pre> <p>then inspect it with:</p> <pre><code>lon status --instance manual-check\n</code></pre> <p>Use <code>lon list</code> to discover registered instances.</p>"},{"location":"troubleshooting/#where-files-live","title":"Where Files Live","text":"<p>Default config path:</p> <pre><code>~/.config/local-n8n/\n</code></pre> <p>Per-instance files:</p> <pre><code>~/.config/local-n8n/instances/&lt;name&gt;/docker-compose.yml\n~/.config/local-n8n/instances/&lt;name&gt;/.env\n</code></pre> <p>State registry:</p> <pre><code>~/.config/local-n8n/state.db\n</code></pre> <p>Override with:</p> <pre><code>LOCAL_N8N_HOME=/tmp/local-n8n-test lon up\n</code></pre>"}]}